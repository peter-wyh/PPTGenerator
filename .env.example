# docker-compose 基础设施变量（compose 从仓库根的 .env 读取做变量替换）
MYSQL_ROOT_PASSWORD=rootpass
# 可选：宿主端口冲突时覆盖（compose 默认 3306/6379）
MYSQL_PORT=3306
REDIS_PORT=6379

# 应用变量（DATABASE_URL / REDIS_URL / JWT_SECRET / PORT）见 apps/server/.env.example
